-#  This file is app/views/movies/index.html.haml
%h1 All Movies

%table#movies
  %thead
    %tr
      #%th= sortable "Movie Title", :id => "title_header"
      // %th{:id => 'title_header', :class => helper_class('title') }= 
      //     link_to "Movie Title", movies_path(:sort=>'title', :ratings =>params[:ratings]), :id => 'title_header'
      
      %th= link_to "Movie Title" :sort => "title_header"
      %th= link_to "Rating" :sort => "release_date_header"
      %th Release Date
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
